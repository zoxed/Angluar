import { InputFormDirective } from './input-form.directive';

describe('InputFormDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFormDirective();
    expect(directive).toBeTruthy();
  });
});
